<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mall.mapper.UserMapper">

    <!-- 登录: 根据用户名查询 -->
    <select id="findByUsername" resultType="com.example.mall.entity.User">
        SELECT id, username, password
        FROM user
        WHERE username = #{username}
        LIMIT 1
    </select>

    <!-- 注册: 新增用户 -->
    <insert id="insert" parameterType="com.example.mall.entity.User">
        INSERT INTO user (username, password, phone, create_time)
        VALUES (#{username}, #{password}, #{phone}, NOW())
    </insert>
</mapper>