<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mall.mapper.OrderMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders (user_id, address_id, total_price, status, create_time)
        VALUES (#{userId}, #{addressId}, #{totalPrice}, #{status}, NOW());
    </insert>

    <select id="findById" resultType="com.example.mall.entity.Order">
        SELECT id, user_id, address_id, total_price, status, create_time
        FROM orders
        WHERE id = #{id}
    </select>

    <update id="updateStatus">
        UPDATE orders
        SET status = #{status}
        WHERE id = #{id}
    </update>

</mapper>

