<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mall.mapper.ProductMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product(name, price, stock, description, image_url, create_time, status)
        VALUES (#{name}, #{price}, #{stock}, #{description}, #{imageUrl}, NOW(), 1)
    </insert>

    <select id="findById" resultType="com.example.mall.entity.Product">
        SELECT
            id,
            name,
            price,
            stock,
            description,
            image_url AS imageUrl,
            create_time AS createTime,
            status
        FROM product WHERE id = #{id}
    </select>

    <select id="findAll" resultType="com.example.mall.entity.Product">
        SELECT
            id,
            name,
            price,
            stock,
            description,
            image_url AS imageUrl,
            create_time AS createTime,
            status
        FROM product ORDER BY create_time DESC
    </select>

    <update id="updateStatus">
        UPDATE product SET status = #{status} WHERE id = #{id}
    </update>
</mapper>